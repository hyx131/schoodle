<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Meta Information -->
  <title>New Events</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0" />

  <!-- External Links -->
  <% include ./partials/_indexLinks %>
</head>

  <body>
    <header>
      <% include ./partials/_header %>
    </header>
    <!-- /* Move all the styling over to the SCSS FILE, these are just here for visual purposes */ -->
    <main class="event-information-container" style="margin: 1em;">
      <div>
        <h3>Event Information</h3>
        <form action="/events" method="POST">
          <div>
            <label for="name">First Name Last Name</label>
            <input
              class="form-control"
              type="userName"
              name="userName"
              placeholder="Enter here"
              style="width: 300px; margin: 0em; margin-top: 0em; margin-bottom: 1em"
            />
          </div>
          <div>
            <label for="email">Email</label>
            <input
              class="form-control"
              type="userEmail"
              name="userEmail"
              placeholder="Enter here"
              style="width: 300px; margin: 0em; margin-top: 0em; margin-bottom: 1em"
            />
          </div>
          <div>
            <label for="title">Event Title</label>

            <input
              class="form-control"
              type="userEvent"
              name="userEvent"
              placeholder="Enter here"
              style="width: 300px; margin: 0em; margin-top: em; margin-bottom: 1em"
            />
          </div>
          <div>
            <label for="description"> Event Description</label>
            <input
              class="form-control"
              type="eventDescription"
              name="eventDescription"
              placeholder="Enter here"
              style="width: 300px; margin: 0em; margin-top: 0em; margin-bottom: 1em"
            />
          </div>
          <div>
            <label for="location">Event Location</label>
            <input
              class="form-control"
              type="eventLocation"
              name="eventLocation"
              placeholder="Enter here"
              style="width: 300px; margin: 0em; margin-top: 0em; margin-bottom: 1em"
            />
          </div>
          <div>
          </div>
        </div>

        <div class='calendar'>
          <h3> (DROP DOWN) Choose Dates & Times </h3>

          <section class='calendar_forms'>
            <section class='date'>
              <div
                id="pick_date"
                name="eventDate">
              </div>
            </section>

            <section class='time_slots'>
              <!-- <p>Jul-25-2019</p>
              <input
              type='text'
              id='start_time'
              name="startTime"
              placeholder='start time'
              />
              <input
              type='text'
              id='end_time'
              name="endTime"
              placeholder='end time'
              /> -->
            </section>

            <!-- <button type="submit" class="btn btn-primary">Continue</button> -->
          <!-- </form> -->
        </section>
      </div>

      <div>
        <h3>DROP DOWN EFFECT - Who Do You Want To Invite? -</h3>
        <!-- <form> -->
          <div>
            <label for="invites">Invite Attendees</label>
            <input
              class="form-control"
              type="invites"
              name="invites"
              placeholder="Enter email here"
              style="width: 300px; margin: 0em; margin-top: 0em; margin-bottom: 1em"
            />
            <input
              class="form-control"
              type="invites"
              name="invites"
              placeholder="Enter email here"
              style="width: 300px; margin: 0em; margin-top: 0em; margin-bottom: 1em"
            />
            <input
              class="form-control"
              type="invites"
              name="invites"
              placeholder="Enter email here"
              style="width: 300px; margin: 0em; margin-top: 0em; margin-bottom: 1em"
            />
            <input
              class="form-control"
              type="invites"
              name="invites"
              placeholder="Enter email here"
              style="width: 300px; margin: 0em; margin-top: 0em; margin-bottom: 1em"
            />
            <!-- <button type="submit" class="btn btn-primary">Continue</button> -->
          </div>
        <!-- </form> -->
      </div>

        <!-- /* Move all the styling over to the SCSS FILE, these are just here for now */ -->

        <div>
          <h1>DROP DOWN EFFECT <b>INVITES</b></h1>
          <label for="invites">Invites</label>
          <input
            class="form-control"
            type="text"
            name="invites"
            placeholder="Enter Email Here"
            style="width: 300px; margin: 1em"
          />
          <button type="submit" class="btn btn-primary">Submit</button>
        </div>
      </form>
    </main>
    <!-- includes Bootstrap/JQuery JS -->
    <% include ./partials/_indexScripts %>






    <!-- Client side jquery for calendar -->
    <script>
      $("#pick_date").flatpickr({
        mode: "multiple",
        inline: true,
        altInput: true,
        altFormat: "F j, Y",
        dateFormat: "Y-m-d",
        "disable": [
          function(date) {
            let today = new Date();
              today.setHours(0,0,0,0);
              return (date < today); //disable past dates >> return true to disable
          }
        ]
      });
      // $("#start_time").flatpickr({
      //   mode: "multiple",
      //   inline: true,
      //   altInput: true,
      //   enableTime: true,
      //   altFormat: "F j, Y",
      //   dateFormat: "Y-m-d",
      //   "disable": [
      //     function(date) {
      //       let today = new Date();
      //         today.setHours(0,0,0,0);
      //         return (date < today); //disable past dates >> return true to disable
      //     }
      //   ]
      // });

      // $("#end_time").flatpickr({
      //   mode: "multiple",
      //   inline: true,
      //   altInput: true,
      //   enableTime: true,
      //   altFormat: "F j, Y",
      //   dateFormat: "Y-m-d",
      //   "disable": [
      //     function(date) {
      //       let today = new Date();
      //         today.setHours(0,0,0,0);
      //         return (date < today); //disable past dates >> return true to disable
      //     }
      //   ]
      // });

      $(document).ready(function() {
        $("#start_time").flatpickr({
          inline: true,
          enableTime: true,
          noCalendar: true,
          dateFormat: "H:i",
        });

        $("#end_time").flatpickr({
          inline: true,
          enableTime: true,
          noCalendar: true,
          dateFormat: "H:i",
        });
      })
    </script>

  </body>
</html>
